# Anchor Rx — AI risk engine (Python 3.13). Direct dependencies only; transitive packages (scipy, starlette,
# anyio, threadpoolctl, ...) are resolved by pip. Versions pinned to the ones the saved model was trained and
# tested with — scikit-learn in particular must match, or train/isolation_forest_pipeline.pkl may not load.
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# API service
fastapi==0.141.1
uvicorn==0.53.0
pydantic==2.13.5

# model + data
scikit-learn==1.9.1
pandas==3.0.5
numpy==2.5.3
joblib==1.6.0

# tests (httpx backs fastapi.testclient.TestClient)
pytest==9.1.1
httpx==0.28.1
